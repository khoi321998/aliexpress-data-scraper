{
    "$schema": "https://apify.com/schemas/v1/input.ide.json",
    "title": "AliExpress Data Scraper",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "mode": {
            "title": "Scrape Mode",
            "type": "string",
            "description": "What to capture. \"Product + Seller (full)\": full product details plus the seller profile, store reviews and product previews. \"Product only\": product details + reviews only, no seller enrichment. \"Seller only\": provide STORE URLs in Start URLs — an independent browser pipeline opens each store page directly on the real local IP (no proxy, no fingerprint) and solves any captcha via 2captcha.",
            "editor": "select",
            "default": "product_and_seller",
            "enum": ["product_and_seller", "product_only", "seller_only"],
            "enumTitles": ["Product + Seller (full)", "Product only", "Seller only (store URLs)"]
        },
        "startUrls": {
            "title": "Start URLs",
            "type": "array",
            "description": "URLs to scrape. For the \"Product\" modes these are product detail pages (https://www.aliexpress.com/item/<id>.html). For \"Seller only\" mode these are store pages (https://www.aliexpress.com/store/<id>).",
            "editor": "requestListSources",
            "prefill": [
                {
                    "url": "https://www.aliexpress.com/item/3256805894825145.html"
                }
            ]
        },
        "shipToCountry": {
            "title": "Ship To Country",
            "type": "string",
            "description": "Two-letter country code (ISO 3166-1 alpha-2, e.g. ES, DE, VN) to use as the delivery destination for every URL. Some listings are only available for certain destinations and return an error when queried for the US. The scraper also routes its proxy through this country, so the delivery address and the connection agree. Leave empty to auto-detect per URL from the address you pasted (e.g. https://es.aliexpress.com/... or ?gatewayAdapt=glo2esp becomes Spain), falling back to US when the URL carries no region.",
            "editor": "textfield"
        },
        "matchStorefrontLocale": {
            "title": "Read Each Product On Its Local Storefront",
            "type": "boolean",
            "description": "Read every product on the delivery country's own storefront, exactly as a shopper there does — Spanish store, prices in EUR, page in Spanish. This is what makes availability truthful: the global catalogue still answers for listings a local storefront has withdrawn, so with this off a product a Spanish shopper cannot order is scraped anyway, priced in USD and with no delivery date. With it on, such a listing is recorded with success = false and errorCode = \"unavailable_in_region\" instead. The trade-off is comparability: prices arrive in each market's own currency and review text in its own language. Turn off to read every region from the global catalogue in USD and English.",
            "default": true
        },
        "residentialProxy": {
            "title": "Use Residential Proxy",
            "type": "boolean",
            "description": "Route ALL traffic through Apify's residential proxy instead of the cheaper datacenter pool. Deliveries outside the US already use residential automatically, because the datacenter pool only holds US addresses. Turn this on to use residential for US deliveries too — datacenter addresses are recognised and challenged by AliExpress far more often. Costs more per request.",
            "default": false
        },
        "headless": {
            "title": "Headless Browser",
            "type": "boolean",
            "description": "Run Chrome in headless (new) mode. Disable to run headful via Xvfb, which is harder to detect but uses more resources.",
            "default": true
        },
        "twoCaptchaApiKey": {
            "title": "2captcha API Key",
            "type": "string",
            "description": "API key for 2captcha (https://2captcha.com). Used by \"Seller only\" mode to solve reCAPTCHA punish pages. Falls back to the TWOCAPTCHA_API_KEY environment variable if left empty.",
            "editor": "textfield",
            "isSecret": true
        }
    },
    "required": ["startUrls"]
}
